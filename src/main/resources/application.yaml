spring:
  application:
    name: onlineshop
  h2:
    console:
      enabled: true
      path: /h2

  datasource:
    url: jdbc:h2:mem:adidb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update


server:
  port: 8080

